{
	"name": "hoyolab-mimo-exchange",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"dev:scheduled": "node -e \"console.log('Open http://localhost:8787/__scheduled to trigger scheduled jobs');\" && wrangler dev --test-scheduled",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.2.0",
		"wrangler": "^4.31.0"
	}
}
